#ALTER USER 'root'@'localhost' IDENTIFIED BY 'P@ssw0rd123$';

#---- OR -----

# Stop MySQL
sudo service mysql stop

# Start MySQL manually, without permission checks or networking.
sudo mysqld_safe --skip-grant-tables --skip-networking &

# Log in without a password.
#mysql -uroot mysql
#UPDATE mysql.user SET authentication_string=PASSWORD('C0mcast!'), plugin='mysql_native_password' WHERE User='root' AND Host='localhost';
#UPDATE mysql.user SET authentication_string=PASSWORD('C0mcast!'), plugin='mysql_native_password' WHERE User='remote' AND Host='%';
#exit

#---- OR -----

#UPDATE mysql.user
#    SET authentication_string = PASSWORD('P@ssw0rd123$'), password_expired = 'N'
#    WHERE User = 'root' AND Host = 'localhost';
#FLUSH PRIVILEGES;


# Stopy MySQL:  /etc/init.d/mysql stop
# mysqld --init-file=/home/ubuntu/reset_mysql_passwd.txt &
